// Configuration for the Remotion Studio + CLI (NOT for your compositions).
// Full list of options: https://www.remotion.dev/docs/config
import { Config } from "@remotion/cli/config";

Config.setVideoImageFormat("jpeg");
Config.setOverwriteOutput(true);
